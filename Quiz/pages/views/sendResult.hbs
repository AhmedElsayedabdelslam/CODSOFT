<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sendresults</title>
   <script src="
https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js
"></script>
    <link href="
https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css
" rel="stylesheet">
    <style>
         body {
            text-align: center;
            color: white;
            
            background-image: url(https://th.bing.com/th/id/R.b9e31289337d40857ea7de26a20b3905?rik=1EyCzwhqlti30g&pid=ImgRaw&r=0);
        }
        label {
            font-size: 35px;
            font-weight: bold;
           
        }
        button {
            background-color: rgb(114, 113, 113);
            color: black;
            width: 200px;
            height: 60px;
            border-radius: 20px;
        }
    </style>
</head>

<body>
    <label style="padding-top: 100px;" for="" id="lb1"></label><br>
    <label for="" id="lb2"></label><br>
    <label for="" id="lb3"></label><br>
    <label for="" id="lb4"></label><br><br><br>
    <button type="button" class="send">Send-Results</button>

    <script>
        const Sendresults = document.querySelector(".send")
        const url = new URL(window.location.href);
        const params = new URLSearchParams(url.search);
        const QuizName = params.get('QuizName')
        const username = params.get('username')
        const myname = params.get('myname')
        const myemail = params.get('myemail')
        const myNumber = params.get('myNumber')
        const score = params.get('score')
        const owner = params.get('owner')
        const lb1 = document.querySelector("#lb1")
        const lb2 = document.querySelector("#lb2")
        const lb3 = document.querySelector("#lb3")
        const lb4 = document.querySelector("#lb4")
        lb1.textContent = "Your Name" + " " + myname
        lb2.textContent = "Your Email" + " " + myemail
        lb3.textContent = "Your Sitting Number" + " " +myNumber
        lb4.textContent = "Your Score is"+ " " + score
        console.log(QuizName, username, myname, myNumber, myemail, score, owner)
        Sendresults.addEventListener("click", () => { 
            console.log("ss")
            alert("You answer has been recevied")
            fetch("http://localhost:3000/sendResult", {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ QuizName: QuizName, username: username, myname: myname, myemail: myemail, owner: owner,score:score, myNumber: myNumber })
            }).then((data) => {
                {{!-- console.log(data) --}}
                {{!-- console.log("Done") --}}
                
            }).catch((e) => {
                {{!-- console.log(e) --}}
            })
        })


    </script>
</body>

</html>